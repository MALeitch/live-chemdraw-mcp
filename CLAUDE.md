See [`AGENTS.md`](AGENTS.md) for this project's development notes —
package layout and the ChemDraw COM quirks this connector was built
around. Read it before making changes to `chemdraw_connector/`.

`AGENTS.md` is the canonical file (tool-agnostic, not Claude-specific);
this file exists only so Claude Code's auto-load convention finds it too.
